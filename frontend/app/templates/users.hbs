<div class="container">
    <div class="row">
        {{#each model as |user|}}
          <div class="col-md-3">
            {{user-widget model=user}}
          </div>
        {{/each}}
    </div>
</div>
