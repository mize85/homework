{{page-toolbar pageTitle='Users'}}
{{#paper-content flex=true scroll-y=true layout-padding=true}}
  {{#doc-content}}
    {{#each model as |user|}}
      {{user.username}}
    {{/each}}
  {{/doc-content}}
{{/paper-content}}